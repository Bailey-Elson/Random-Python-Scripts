# String value that needs copying
text = """

"""

# Array of all repalcement words
replacements = []

# Keyword to replace
keyword = ""

# Loop through replacements and print the updated string
for replacement in replacements:
    updated_text = text.replace(keyword, replacement)
    print(updated_text)

print(sqltext)
